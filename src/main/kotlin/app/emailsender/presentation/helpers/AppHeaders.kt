package app.emailsender.presentation.helpers

class AppHeaders {
    companion object {
        const val X_USER_ID = "X-User-Id"
        const val X_USER_SCOPES = "X-User-Scopes"
        const val X_USER_ROLES = "X-User-Roles"
    }
}